# homebutler.dev — 이 사이트를 고칠 때 지키는 것

이 문서의 규칙은 전부 **실제로 난 사고**에서 나왔다. 지어낸 규칙은 없다.
규칙마다 "왜"가 붙어 있는데, 이유를 잃으면 다음 사람에게는 그냥 번거로움이
되기 때문이다. 이유가 더 이상 참이 아니면 규칙을 지워라.

형제 제품 PortButler의 `portbutler-site/RULES.md`와 같은 뼈대를 쓴다. 세
번째 제품이 나올 때 둘이 같은 모양인 것이 그때 값을 한다.

## 1. 푸시하면 배포된다. 그러니 PR로 올린다

`main`에 들어가는 순간 homebutler.dev가 바뀐다. 되돌리는 데 드는 시간이
아니라 **그사이 누가 보느냐**가 문제다.

**왜**: 유튜버 넷에게 연락을 보낸 날, 사이트에는 `no SSH key required`가
떠 있었다. 확인하러 온 사람이 첫 화면에서 거짓말을 읽을 수 있는 상태였다.

## 2. 제품이 안 하는 말을 사이트가 하지 않는다

사이트 문구는 제품 저장소의 README·docs와 **같은 사실**을 말해야 한다.
한쪽만 고치는 것이 이 저장소에서 가장 자주 나는 사고다.

**왜**: `no SSH key required`는 거짓이었다. 원격 서버는 SSH 키로 붙고,
바이너리는 `homebutler deploy`로 한 번 올려 둔다. README에서는 0.35.2에
걷어냈는데 사이트에만 두 판을 더 남아 있었다. 같은 사실을 두 곳에 적으면
반드시 한쪽만 고쳐진다.

**어떻게**: 문구를 고칠 때 제품 저장소에서 같은 문장을 `git grep` 한다.
없으면 새 주장이다. 새 주장은 제품 쪽에 먼저 쓰고 사이트가 따라간다.

## 3. 자산은 원본이 한 곳이어야 한다 — 그리고 복사보다 생성이 낫다

스크린샷·GIF·og 이미지의 **원본은 제품 저장소**다. 사이트는 그것을
가져오되, 손으로 옮기지 않는다.

**더 나은 것**: 사본을 옮기는 대신 **캡처 원문에서 매번 다시 굽는다.**
데모 영상 장면을 그렇게 만들었다 — `captures/*.txt`가 명령의 실제 출력이고
생성기가 그것을 읽어 화면을 그린다. 사람이 옮겨 적는 단계가 없으면 두
사본이 갈라질 자리도 없다.

**왜**: `public/images/install-demo.gif`는 제품 저장소에서 **지워진 뒤에도**
여기 살아 있었다. 그 GIF는 존재하지 않는 명령(`homebutler logs`)을 가르치고,
앱 개수를 5개로 적고(실제 15개), 만든 사람의 홈 디렉터리 경로가 찍혀 있었다.
제품에서 지운 자산이 공개처에 남아 있는 것은 사고다.

**규칙**: 제품에서 지운 자산은 여기서도 지운다. 안 쓰는 사본을 남겨 두면
그게 다시 살아난다.

## 4. 수치는 적지 않는다. 적어야 하면 사람이 타이핑하지 않게 한다

버전·스타 수·도구 개수처럼 **판마다 바뀌는 값은 사이트에 적지 않는다.**
적으면 판마다 고쳐야 하고, 안 고치면 그 자리가 조용히 거짓말을 시작한다.

**예외는 안전 때문에 버전을 적어야 하는 자리뿐이다.** `go install …@latest`는
"지금 최신인 실행 파일을 가져와라"라서 핀 되지 않은 설치다 — ClawHub 보안
스캐너가 우리 스킬에서 정확히 그걸 지적했다. 그래서 설치 명령에는 버전을
적는다. 대신 **그 값을 사람이 타이핑하게 두지 않는다**: 빌드 때 릴리스에서
읽어 채우는 것이 목표다(아직 안 했다. 지금은 릴리스 체크리스트에 있다).

**왜**: `DESIGN-BRIEF.md`가 v0.13.0·⭐81·MCP 15개 기준으로 다섯 달을
지냈다. 그동안 실제로는 0.35.2·282·44였다.

## 5. 문구가 제품과 어긋나는 걸 잡는 장치가 없다 — 그래서 사람이 읽는다

빌드는 생김새만 본다. **문구가 참인지는 한 줄도 검사하지 않는다.**

제품 저장소에는 장치가 있다. `cmd/skill_test.go`는 문서의 모든 `homebutler …`
줄을 cobra 명령 트리에 해석시키고, 없는 명령이나 없는 플래그면 빌드를
깨뜨린다. 도움말 텍스트를 파싱하지 않는 이유는 도움말도 같이 낡기 때문이다.
**사이트에도 같은 종류를 붙일 수 있다** — 설치 명령을 실제로 실행해 보는
것이 시작이다. 아직 안 했다.

그때까지는 판마다 읽는 목록이다. 체크리스트는 검사가 아니고 사람이 건너뛸
수 있다는 것을 알고 쓴다.

```
[ ] 첫 화면의 주장이 제품 저장소에도 같은 문장으로 있는가
[ ] "없다/불필요하다"류 문장을 코드에서 확인했는가 (에이전트? 데몬? 키?)
[ ] 설치 명령을 그대로 복사해 실행하면 도는가 (macOS와 리눅스 양쪽)
[ ] 화면 이미지가 지금 제품의 화면인가 — 탭 이름·색·항목까지
[ ] 아직 안 나간 기능을 현재형으로 쓰고 있지 않은가
[ ] 제품에서 지운 자산이 public/ 에 남아 있지 않은가
```

**왜**: 오늘 잡힌 사실 오류는 전부 사람이 읽다가 잡았다. 빌드는 네 건 모두
통과시켰다.

## 6. "어디서 읽었는가"도 전제다

사이트는 **검색·리뷰 영상·뉴스레터**를 통해 도착한 사람이 읽는다. 저장소를
먼저 본 사람이 아니다. 그래서 첫 화면은 맥락 없이 읽혀야 하고, 우리끼리
쓰는 말(내부 이슈 번호, 로드맵 단계)은 여기 오지 않는다.

**왜**: 최근 2주 유입의 대부분이 selfh.st 뉴스레터와 검색이었다. 저장소에서
넘어온 사람은 그보다 적다.

## 7. 고칠 때는 사람이 도착하는 순서로 훑는다

검색 → **사이트** → 릴리스 페이지 → 저장소 → 스킬·레지스트리.

**왜**: 목록의 마지막에 둔 것이 항상 빠진다. PortButler 쪽은 사이트만
고치고 다운로드 페이지를 못 고쳐서, 고객이 실제로 도착하는 마지막 지점이
낡은 채 남았다. 우리는 README를 고치고 사이트를 하루 늦게 고쳤다. 같은 병이다.

## 8. 되돌릴 수 없는 것

- **배포된 화면은 누가 이미 봤다.** 되돌려도 본 사람은 되돌려지지 않는다.
- **og:image는 캐시된다.** 소셜에 한 번 퍼진 카드 이미지는 우리가 바꿔도
  한동안 옛 것이 돌아다닌다.
- **유튜브 영상은 파일을 교체할 수 없다.** 문구·썸네일은 고칠 수 있지만
  영상 자체는 새로 올려야 하고 주소가 바뀐다.
